#ifndef CONSTS_H
#define CONSTS_H

#define loop_x for(int x = 0; x < 9; ++x)
#define loop_y for(int y = 0; y < 9; ++y)
#define loop(x,n) for(int x = 0; x < n; ++x)

class Consts
{
    public:
        Consts();
        virtual ~Consts();

    protected:

    private:
};

#endif // CONSTS_H
